
---

After analyzing the source code, I found several functions such as `getRandomNumber()` and `esoteric1()`. After connecting to the listener, it prompted that if we use `getRandomNumber`, which is also present in the source code, it should return a random number. There were other functions as well, which I tested individually.

When I tried calling `win()`, I received the flag in hexadecimal format:

```
0x70 0x69 0x63 0x6f 0x43 0x54 0x46 0x7b 0x34 0x5f 0x64 0x31 0x34 0x6d 0x30 0x6e 0x64 0x5f 0x31 0x6e 0x5f 0x37 0x68 0x33 0x5f 0x72 0x30 0x75 0x67 0x68 0x5f 0x63 0x65 0x34 0x62 0x35 0x64 0x35 0x62 0x7d
```

Each hex value was converted into ASCII characters to reveal the final flag.

