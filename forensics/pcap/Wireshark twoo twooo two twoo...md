I opened the file in **Wireshark**. After analyzing all the packets and following the **TCP streams**, I noticed that there were too many fake flags in **TCP streams 6 and 11**.

Then I checked the IP addresses and found that the **most common IP address was 18.217.1.57**, so I filtered the traffic using:

```
ip.addr == 18.217.1.57 && http
```

While analyzing further, I noticed that there were many **DNS requests** for:

```
<some_gibberish>.redshrimpandherring.com
```

So I applied another filter:

```
ip.addr == 18.217.1.57 && dns.qry.name contains local
```

After that, I copied all the relevant packets and saved them into a **.txt file**. Then I used a **Python script generated by Claude AI** to decode the data.

I also referred to another person's write-up because I couldn't understand which flag was correct. I spent around **40 minutes** confused because my mind just wasn’t working at that moment.

https://medium.com/@matus.vaclav1/picoctf-wireshark-twoo-twooo-two-twoo-7f00d1a923b4
